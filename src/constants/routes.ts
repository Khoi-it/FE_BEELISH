export const ROUTES = {
  LANDING: '/',
  HOME: '/home',
  VIDEO: '/video',
  DICTATION: '/dictation',
  VOCABULARY: '/vocabulary',
  LOGIN: '/login',
  REGISTER: '/register',
  PROFILE: '/profile',
}

export function routeHash(route: string) {
  return route
}

